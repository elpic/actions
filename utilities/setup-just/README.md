# Setup Just

Install the [just](https://just.systems) command runner.

Skips installation if `just` is already available on the runner.

## Usage

```yaml
- uses: elpic/actions/utilities/setup-just@v1
- run: just build
```
